#include <iostream>

int main () {
    std::cin.tie(NULL);
    std::ios::sync_with_stdio(false);
    long long int a, b;
    long long int sum;

    std::cin >> a >> b;

    sum = a + b;

    std::cout << sum;

}